ns some_ns;

use sf house (move_house, move_in_one, evict_one, house_information);

fn main(house: House, target_address: String, humans: Vec<Human>) -> House {
    let house2 = move_house(house, target_address);
    let (house3, humans2) = move_in_one(house2, humans);
    let (house4, humans3) = move_in_one(house3, humans2);
    let (house5, humans4) = move_in_one(house4, humans3);

    house_information(house5);

    evict_one(house5)
}
