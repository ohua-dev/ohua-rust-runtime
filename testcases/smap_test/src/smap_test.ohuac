ns some_ns;

use sf smapping (gen_input, printout, splice);

fn main() -> Vec<String> {
    let input = gen_input();
    for i in input {
        let s = splice(i);
        printout(s);
        s
    }
}
