ns some_ns;

use sf main_arguments::mainclone::{expand_content, splice_message};

fn main(input: String) -> String {
    let intermediate = expand_content(input);
    splice_message(intermediate, input)
}
