ns simple_tl_recur;

use sf recursion::sfs::{lt, plus};

fn go (n:i32) {
   let next = plus(n0,3);
   if (lt(n0, 10)) {
     go(next)
   } else {
     n0
   }
}

fn main() {
   ohua::lang::id(go(0))
}