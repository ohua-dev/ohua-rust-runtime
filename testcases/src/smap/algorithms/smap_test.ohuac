ns some_ns;

use sf smap::smap_fns::{gen_input, printout, splice};

fn main() -> Vec<String> {
    let input = gen_input();
    for i in input {
        let s = splice(i);
        printout(s);
        s
    }
}
