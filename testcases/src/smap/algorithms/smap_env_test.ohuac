ns some_ns;

use sf crate::smap::smap_fns::{gen_input, printout, splice};

fn main(input: Vec<String>) -> Vec<String> {
    for i in input {
        let s = splice(i);
        printout(s);
        s
    }
}
