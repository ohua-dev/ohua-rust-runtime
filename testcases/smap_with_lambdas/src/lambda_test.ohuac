ns some_ns;

use sf calculations (generate_value, generate_data, calculate);

fn main() -> Vec<i32> {
    let x = generate_value();
    let y = generate_data();

    let f = fn(d) {calculate(d, x)};

    for num in y {
        f(num)
    }
}
