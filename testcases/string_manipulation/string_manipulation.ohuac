ns some_ns;

use sf strings (extend_string1, extend_string2, append, duplicate, count);

fn main(initial: String) -> usize {
    let s1 = extend_string1(initial);
    let s2 = extend_string2(initial);

    let s3 = append(s1, s2);
    let dup = duplicate(s3);
    count(dup)
}
