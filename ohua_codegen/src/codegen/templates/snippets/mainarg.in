
pub fn mainarg{n}(mut args: Vec<Box<dyn Any + 'static + Send>>) -> Vec<Vec<Box<dyn Any + 'static + Send>>> {
    let arg: {arg_type} = *args.pop().unwrap().downcast().unwrap();

    vec![vec![{argument}]]
}
